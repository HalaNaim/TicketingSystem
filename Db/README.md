# Ticketing System Database Schema

This repository contains the SQL schema for the Ticketing System project.

## How to use
1. Open SQL Server Management Studio (SSMS).
2. Create a new database (e.g., TicketingSystem).
3. Run the `schema.sql` script to create all tables.

